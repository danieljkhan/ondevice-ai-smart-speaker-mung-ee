-r ./Dev_Plan/requirements-dev.txt
rich>=13.7.0
anthropic>=0.40.0
# Phase A child-filter judging (dev-only; not deployed to Jetson):
google-genai>=2.0.0          # Gemini API direct (Google AI Studio) -- Phase A primary judge (Plan v1.5, ADR 0092)
pyarrow>=15.0.0              # Parquet reader for raw Wikipedia ingest (rahular/simple-wikipedia, lcw99/wikipedia-korean-20240501)
openai>=1.50.0              # Phase A.1 KO judge -- GPT-4.1 mini batch judge (Plan v1.5 fallback path)
playwright>=1.44.0          # Offline CSS/HTML emoji frame capture (dev/CI only; not deployed to Jetson)
fonttools>=4.50.0           # History UI font subset generation and coverage validation
